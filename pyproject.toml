[project]
name = "DRIVE"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "shapely",
    "numpy",
    "black",
    "pytest",
    "matplotlib",
    "pointpats"
]

[tool.black]
line-length = 120
